# --- Cấu hình ứng dụng ---
APP_NAME=IoT_Ingestion_Service
APP_ENV=development
PORT=8000

# --- Cấu hình Database/Dịch vụ (Ví dụ) ---
# Tuyệt đối không điền mật khẩu thật vào file này!
DB_HOST=localhost
DB_PORT=5432
DB_USER=example_user
DB_PASSWORD=your_secret_password_here